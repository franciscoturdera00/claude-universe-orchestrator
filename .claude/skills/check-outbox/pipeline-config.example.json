{
  "_comment": "Operator-specific pipeline dashboard config. Copy to pipeline-config.json (gitignored), then populate notion_page_id / notion_database_url / data_source_id with the IDs returned when you create the dashboard via Notion MCP. project_rows is the row-id cache: leave empty on first run; the check-outbox skill writes new entries here as projects are added.",
  "notion_page_id": "REPLACE_WITH_LILO_PIPELINE_PAGE_UUID",
  "notion_page_url": "https://www.notion.so/REPLACE_WITH_PAGE_URL",
  "notion_database_url": "https://www.notion.so/REPLACE_WITH_DATABASE_URL",
  "data_source_id": "REPLACE_WITH_PROJECTS_DATA_SOURCE_UUID",
  "pipeline_md_path": "pipeline.md",
  "pipeline_json_path": "pipeline.json",
  "project_rows": {}
}
