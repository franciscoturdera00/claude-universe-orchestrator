{
  "_note": "Copy this to .mcp.json on first clone, then customize. .mcp.json is gitignored — add any local MCPs you want (e.g. ios-simulator on macOS) without worrying about portability.",
  "mcpServers": {
    "claude-universe-tools": {
      "command": "./tools/mcp-bridge/.venv/bin/python",
      "args": [
        "./tools/mcp-bridge/server.py"
      ]
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--headless", "--isolated"]
    }
  }
}
